<Window x:Class="FloatingReminder.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FloatingReminder"
        mc:Ignorable="d"
        Title="Login - Floating Reminder" 
        Height="450" Width="350"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="White">

    <Window.Resources>
        <LinearGradientBrush x:Key="DashboardBackgroundBrush" StartPoint="0.5,0" EndPoint="0.5,1">
            <GradientStop Color="#854F6C" Offset="0.0"/>
            <GradientStop Color="#190019" Offset="1.0"/>
        </LinearGradientBrush>
        <Style x:Key="GlassTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#11FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="8" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1a2a6c"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="20" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#22FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,8"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" Padding="1" Background="Transparent">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Color="#000000" Opacity="0.4" ShadowDepth="0"/>
        </Border.Effect>

        <Grid x:Name="BackgroundGrid" Background="{StaticResource DashboardBackgroundBrush}">
            <Grid.Clip>
                <RectangleGeometry RadiusX="20" RadiusY="20" Rect="0,0,350,450"/>
            </Grid.Clip>

            <Border Background="Transparent" MouseLeftButtonDown="Border_MouseLeftButtonDown" />

            <Button Content="×" FontSize="16" Width="30" Height="30"
                    Style="{StaticResource SecondaryButton}"
                    VerticalAlignment="Top" HorizontalAlignment="Right"
                    Margin="0,15,15,0"
                    Click="CloseButton_Click"/>

            <StackPanel Margin="30">
                <TextBlock Text="Welcome" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,20"/>

                <TextBlock x:Name="SuccessText" Foreground="#90EE90" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,10" 
                           TextWrapping="Wrap" Visibility="Collapsed"/>

                <TextBlock Text="Username" FontSize="12" Opacity="0.7" Margin="0,0,0,5"/>
                <TextBox x:Name="UsernameBox" Style="{StaticResource GlassTextBox}" Margin="0,0,0,15"/>

                <TextBlock Text="Password" FontSize="12" Opacity="0.7" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox"
                             Background="#11FFFFFF" BorderThickness="0" Foreground="White"
                             CaretBrush="White" Padding="10" VerticalContentAlignment="Center">
                    <PasswordBox.Template>
                        <ControlTemplate TargetType="PasswordBox">
                            <Border CornerRadius="8" Background="{TemplateBinding Background}">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                        </ControlTemplate>
                    </PasswordBox.Template>
                </PasswordBox>

                <TextBlock x:Name="ErrorText" Foreground="#FF6B6B" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,10,0,0" 
                           TextWrapping="Wrap" Visibility="Collapsed"/>

                <Button x:Name="LoginButton" Content="Login" 
                        Style="{StaticResource PrimaryButton}" 
                        Margin="0,20,0,0"
                        Click="LoginButton_Click"/>

                <Grid Margin="0,30,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="GuestButton" Grid.Column="0" Content="Continue as Guest" 
                            Background="Transparent" BorderThickness="0" Foreground="#AAFFFFFF"
                            Cursor="Hand" FontSize="11" HorizontalAlignment="Left"
                            Click="GuestButton_Click"/>

                    <Button x:Name="CreateAccountButton" Grid.Column="1" Content="Create Account" 
                            Background="Transparent" BorderThickness="0" Foreground="#AAFFFFFF"
                            Cursor="Hand" FontSize="11" HorizontalAlignment="Right"
                            Click="CreateAccountButton_Click"/>
                </Grid>

            </StackPanel>
        </Grid>
    </Border>
</Window>